export async function handleUserCallbacks(chatId:number,data:string){

 // user panel callbacks yahan handle honge

 if(data === "page_team"){
   // team page
 }

 if(data === "page_settings"){
   // settings
 }

}